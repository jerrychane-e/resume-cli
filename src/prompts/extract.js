// 简历提取 Prompt 模板

export const 简历提取系统提示词 = '你是一个精确的简历信息提取器，严格按要求返回 JSON，不做多余解释，不改变字段名称。';

export function 构建简历提取用户提示词(简历文本) {
  return `请从以下简历文本中提取信息，严格返回如下 JSON 结构，不要 markdown 代码块，不要额外文字。

{
  "name": "姓名（中文默认真实姓名，英文名保留原样）",
  "phone": "手机号码（仅数字，无分隔符，若没有则为空字符串）",
  "email": "电子邮箱地址（小写）",
  "city": "期望工作城市或现居城市",
  "education": [
    {
      "school": "学校全称（不要缩写）",
      "major": "专业名称",
      "degree": "学历学位（如 本科、硕士、博士）",
      "graduation_time": "毕业时间（格式 YYYY-MM 或 YYYY，若无法确定则为空字符串）"
    }
  ],
  "skills": ["技能名称1", "技能名称2"]
}

简历文本如下：
${简历文本}`;
}
